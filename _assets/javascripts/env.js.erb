// Main application IoC container
var STS = {
  options: {
    ABSURL                  : '<%= site.url %>',
    BASEURL                 : '<%= site.baseurl %>',

    ENABLE_REALTIME         : <%= site.config['enable_realtime_features'] %>,
    SOCKET_CONNECT_TIMEOUT  : <%= site.config['realtime_connection_timeout'] %>,

    LEGISLATORS_LOCATOR_URL : '<%= site.config['legislators_locator_url'] %>',

    API_BASE_URL            : '<%= site.config['api_url'] %>',
    API_SOCKET_BASEURL      : '<%= site.config['api_socket_path'] %>',

    TWEETS_READ_URL         : '<%= site.config['api_url'] + site.config['tweets_read_path'] %>',
    STATS_READ_URL          : '<%= site.config['api_url'] + site.config['stats_read_path'] %>',
    SHARES_READ_URL          : '<%= site.config['api_url'] + site.config['shares_read_path'] %>',

    SEND_EMAIL_URL          : '<%= site.config['api_url'] + site.config['send_email_path'] %>',
    LOG_URL_BASE            : '<%= site.config['api_url'] + site.config['log_path'] %>',

    USER_PROGRESS_COOKIE_LIFETIME : 28 * 24 * 3600 // ~1 month
  },
  events: {}
};
